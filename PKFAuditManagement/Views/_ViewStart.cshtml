@{
    var user = Context.User;
    string layout = "_Layout";
    if (user.IsInRole("Admin"))
    {
        layout = "_AdminLayout";
    }
    else 
    {
        layout = "_UserLayout";
    }

    Layout = layout;
}
