@{
    var user = Context.User;
    string layout = "_Layout";
    if (user.IsInRole("Admin") || user.IsInRole("Reviewer"))
    {
        layout = "_AdminLayout";
    }
    else 
    {
        layout = "_UserLayout";
    }

    Layout = layout;
}
